# 小文智能助手 — 用户系统模块开发计划

> 负责人：刘汉文 | 日期：2026-05-30 | 版本 v1.0
>
> 基于《项目优化方案》第 2.1、2.2、3.2 节及日志需求细化。

---

## 目录

- [一、模块概览与依赖关系](#一模块概览与依赖关系)
- [二、技术栈总览](#二技术栈总览)
- [三、阶段一：数据库与后端认证系统（2 天）](#三阶段一数据库与后端认证系统2-天)
- [四、阶段二：登录页 3D 动画（1.5 天）](#四阶段二登录页-3d-动画15-天)
- [五、阶段三：登录注册前端闭环（1 天）](#五阶段三登录注册前端闭环1-天)
- [六、阶段四：个人偏好系统（1.5 天）](#六阶段四个人偏好系统15-天)
- [七、阶段五：用户日志系统（1.5 天）](#七阶段五用户日志系统15-天)
- [八、阶段六：联调与集成测试（1 天）](#八阶段六联调与集成测试1-天)
- [九、总时间线与里程碑](#九总时间线与里程碑)
- [十、风险与应对](#十风险与应对)

---

## 一、模块概览与依赖关系

### 1.1 五个模块

| 编号 | 模块 | 说明 |
|------|------|------|
| M1 | JWT 用户认证系统 | Flask-JWT-Extended + SQLite + AuthContext |
| M2 | 登录页 3D 动画 | Three.js 粒子星空 + 鼠标视差 |
| M3 | 用户登录/注册完整流程 | 前后端打通，路由守卫 + 自动 Token 注入 |
| M4 | 个人偏好系统 | 偏好注入系统提示 + 设置面板 UI |
| M5 | 用户日志系统 | 对话日志按用户隔离存储、登录后可访问 |

### 1.2 依赖关系图

```
        ┌──────────────┐
        │ M1: JWT 认证  │ ← 基石
        └──────┬───────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌───────┐
│  M3   │ │  M4   │ │  M5   │
│登录注册│ │偏好系统│ │日志系统│
└───┬───┘ └───────┘ └───────┘
    │
    ▼
┌───────┐
│  M2   │  ← 依赖 M3 的登录页 UI，但 Three.js 动画可独立开发
│3D 动画│
└───────┘
```

**关键依赖说明**：
- M1 是所有模块的**硬依赖**，必须先完成（后端 JWT 认证 + SQLite 建表）
- M3 依赖 M1（需要认证 API），与 M2 可**并行开发**（M2 做独立的 Three.js 组件，M3 做登录表单逻辑，最后整合）
- M4 依赖 M1 + M3（需要用户已登录才能设置偏好）
- M5 依赖 M1 + M3（需要按 user_id 隔离日志）

### 1.3 推荐执行顺序

```
阶段一 → 阶段二/三并行 → 阶段四 → 阶段五 → 阶段六（联调）
 M1        M2 + M3        M4        M5         整体测试
```

---

## 二、技术栈总览

| 层 | 技术 | 版本/说明 |
|----|------|-----------|
| **后端框架** | Flask | 已有 |
| **JWT 认证** | Flask-JWT-Extended | `pip install flask-jwt-extended` |
| **密码哈希** | werkzeug.security | Flask 内置，generate_password_hash / check_password_hash |
| **数据库** | SQLite（sqlite3） | Python 内置，后期可升 PostgreSQL |
| **3D 渲染** | Three.js | `pnpm add three`（CDN 按需加载可优化首屏） |
| **前端认证** | React Context（AuthContext） | 已有 React 技术栈 |
| **HTTP 客户端** | fetch + apiBase.js | 需改造，自动注入 Authorization header |
| **偏好存储** | SQLite（user_preferences 表）| 键值对模式 |
| **日志存储** | SQLite（user_logs 表）| 按 user_id 索引 |

---

## 三、阶段一：数据库与后端认证系统（2 天）

### 3.1 任务清单

| 任务编号 | 任务 | 预估 | 产出物 |
|----------|------|------|--------|
| T1.1 | 设计并创建 SQLite 数据库表 | 2h | `backend/models/user.py` |
| T1.2 | 实现 `/api/auth/register` | 2h | `backend/routes/auth.py` |
| T1.3 | 实现 `/api/auth/login` | 1.5h | 同上 |
| T1.4 | 实现 `/api/auth/me`（获取当前用户） | 1h | 同上 |
| T1.5 | 改造 `app.py` 注册 JWT 和蓝图 | 1h | `backend/app.py` |
| T1.6 | 为 `/api/send-task` 等业务接口加 `@jwt_required()` | 1.5h | `backend/routes/api.py` |
| T1.7 | 编写认证 API 测试脚本 | 1h | `backend/tests/test_auth.py` |

### 3.2 数据库表设计

```sql
-- backend/models/user.py
-- 用户表
CREATE TABLE IF NOT EXISTS users (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    username    TEXT    UNIQUE NOT NULL,
    password    TEXT    NOT NULL,          -- bcrypt 哈希
    display_name TEXT   DEFAULT '',
    avatar_url  TEXT    DEFAULT '',
    role        TEXT    DEFAULT '',        -- 职业/角色
    created_at  TEXT    DEFAULT (datetime('now', 'localtime')),
    updated_at  TEXT    DEFAULT (datetime('now', 'localtime'))
);

-- 用户偏好表（阶段四用，此阶段先建表）
CREATE TABLE IF NOT EXISTS user_preferences (
    user_id     INTEGER NOT NULL,
    pref_key    TEXT    NOT NULL,          -- 偏好键名
    pref_value  TEXT    NOT NULL,          -- 偏好值
    updated_at  TEXT    DEFAULT (datetime('now', 'localtime')),
    PRIMARY KEY (user_id, pref_key),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- 用户日志表（阶段五用，此阶段先建表）
CREATE TABLE IF NOT EXISTS user_logs (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     INTEGER NOT NULL,
    session_id  TEXT    NOT NULL,
    role        TEXT    NOT NULL,          -- 'user' | 'assistant' | 'system'
    content     TEXT    NOT NULL,
    intent      TEXT    DEFAULT '',
    metadata    TEXT    DEFAULT '{}',      -- JSON 扩展字段
    created_at  TEXT    DEFAULT (datetime('now', 'localtime')),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_logs_user_id ON user_logs(user_id);
CREATE INDEX IF NOT EXISTS idx_logs_user_session ON user_logs(user_id, session_id);
CREATE INDEX IF NOT EXISTS idx_logs_created ON user_logs(user_id, created_at);
```

### 3.3 T1.1：数据库初始化模块

`backend/models/user.py` 核心代码：

```python
import sqlite3
import os

DB_PATH = os.path.join(os.path.dirname(__file__), '..', 'xiaowen.db')

def get_db():
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA foreign_keys=ON")
    return conn

def init_db():
    conn = get_db()
    conn.executescript("""
        CREATE TABLE IF NOT EXISTS users (...);
        CREATE TABLE IF NOT EXISTS user_preferences (...);
        CREATE TABLE IF NOT EXISTS user_logs (...);
        -- 索引
    """)
    conn.commit()
    conn.close()
```

### 3.4 T1.2-1.4：认证路由

`backend/routes/auth.py` 核心代码：

```python
from flask import Blueprint, request, jsonify
from flask_jwt_extended import (
    create_access_token, jwt_required, get_jwt_identity
)
from werkzeug.security import generate_password_hash, check_password_hash
from models.user import get_db
import re

auth_bp = Blueprint("auth", __name__, url_prefix="/api/auth")

# ====== 注册 ======
@auth_bp.route("/register", methods=["POST"])
def register():
    data = request.get_json()
    username = data.get("username", "").strip()
    password = data.get("password", "")
    display_name = data.get("displayName", username)

    # 校验
    if not username or not password:
        return jsonify({"error": "用户名和密码不能为空"}), 400
    if len(username) < 3 or len(username) > 20:
        return jsonify({"error": "用户名长度 3-20 个字符"}), 400
    if len(password) < 6:
        return jsonify({"error": "密码至少 6 位"}), 400
    if not re.match(r'^[a-zA-Z0-9_\u4e00-\u9fff]+$', username):
        return jsonify({"error": "用户名只能包含字母/数字/下划线/中文"}), 400

    conn = get_db()
    existing = conn.execute("SELECT id FROM users WHERE username = ?", (username,)).fetchone()
    if existing:
        conn.close()
        return jsonify({"error": "用户名已存在"}), 409

    hashed = generate_password_hash(password)
    cursor = conn.execute(
        "INSERT INTO users (username, password, display_name) VALUES (?, ?, ?)",
        (username, hashed, display_name)
    )
    user_id = cursor.lastrowid
    conn.commit()
    conn.close()

    token = create_access_token(identity=str(user_id))
    return jsonify({
        "token": token,
        "user": {"id": user_id, "username": username, "displayName": display_name}
    }), 201

# ====== 登录 ======
@auth_bp.route("/login", methods=["POST"])
def login():
    data = request.get_json()
    username = data.get("username", "").strip()
    password = data.get("password", "")

    if not username or not password:
        return jsonify({"error": "用户名和密码不能为空"}), 400

    conn = get_db()
    user = conn.execute("SELECT * FROM users WHERE username = ?", (username,)).fetchone()
    conn.close()

    if not user or not check_password_hash(user["password"], password):
        return jsonify({"error": "用户名或密码错误"}), 401

    token = create_access_token(identity=str(user["id"]))
    return jsonify({
        "token": token,
        "user": {
            "id": user["id"],
            "username": user["username"],
            "displayName": user["display_name"],
        }
    })

# ====== 获取当前用户 ======
@auth_bp.route("/me", methods=["GET"])
@jwt_required()
def get_me():
    user_id = int(get_jwt_identity())
    conn = get_db()
    user = conn.execute("SELECT * FROM users WHERE id = ?", (user_id,)).fetchone()
    conn.close()
    if not user:
        return jsonify({"error": "用户不存在"}), 404
    return jsonify({
        "user": {
            "id": user["id"],
            "username": user["username"],
            "displayName": user["display_name"],
            "role": user["role"],
            "avatarUrl": user["avatar_url"],
        }
    })
```

### 3.5 T1.5：改造 app.py

```python
# backend/app.py（改造后）
from flask import Flask
from flask_cors import CORS
from flask_jwt_extended import JWTManager
from models.user import init_db
from routes.auth import auth_bp
from routes.api import api_bp
from routes.preferences import pref_bp    # 阶段四启用
from routes.logs import logs_bp          # 阶段五启用
import os

app = Flask(__name__)
CORS(app, resources={r"/api/*": {"origins": "*"}}, expose_headers=["*"])

app.config["JWT_SECRET_KEY"] = os.getenv("JWT_SECRET_KEY", "dev-secret-change-in-production")
app.config["JWT_ACCESS_TOKEN_EXPIRES"] = 86400  # 24 小时
jwt = JWTManager(app)

init_db()

app.register_blueprint(auth_bp)   # 公开
app.register_blueprint(api_bp)    # 需认证
# app.register_blueprint(pref_bp)   # 阶段四取消注释
# app.register_blueprint(logs_bp)   # 阶段五取消注释

if __name__ == "__main__":
    app.run(host="127.0.0.1", port=5001, debug=True)
```

### 3.6 T1.6：保护业务 API

为 `backend/routes/api.py` 中所有业务接口添加 `@jwt_required()` 并提取 `user_id`：

```python
from flask_jwt_extended import jwt_required, get_jwt_identity

@api_bp.route("/send-task", methods=["POST"])
@jwt_required()
def send_task():
    user_id = int(get_jwt_identity())
    data = request.get_json()
    task = data.get("task", "").strip()
    # ... 原有逻辑，注意日志写入阶段五再做

@api_bp.route("/tts", methods=["POST"])
@jwt_required()
def tts():
    # ...

@api_bp.route("/analyze-image", methods=["POST"])
@jwt_required()
def analyze_image():
    # ...
```

---

## 四、阶段二：登录页 3D 动画（1.5 天）

### 4.1 任务清单

| 任务编号 | 任务 | 预估 | 产出物 |
|----------|------|------|--------|
| T2.1 | 安装 Three.js，创建 LoginParticleBg 组件 | 3h | `src/components/LoginParticleBg.jsx` |
| T2.2 | 实现粒子星空：球形分布 + 多色粒子 + 缓慢旋转 | 2h | 同上 |
| T2.3 | 实现鼠标视差效果 | 1.5h | 同上 |
| T2.4 | 性能优化：像素比限制、低端设备降级、资源清理 | 1.5h | 同上 |
| T2.5 | 编写 3D 动画 CSS 层叠样式 | 1h | `src/components/LoginParticleBg.css` |

### 4.2 技术要点

**核心组件架构**：

```
LoginParticleBg.jsx
├── useEffect: Three.js 初始化
│   ├── 场景: Scene + PerspectiveCamera + WebGLRenderer
│   ├── 粒子: BufferGeometry（800 粒子，球形分布）
│   ├── 材质: PointsMaterial（AdditiveBlending, vertexColors）
│   └── 纹理: 圆形渐变 Canvas 纹理
├── useEffect: 动画循环 (requestAnimationFrame)
│   ├── 粒子自转 (rotation.x/y 缓慢递增)
│   └── 鼠标视差 (lerp 平滑插值)
├── 事件: mousemove → mouseRef 更新
├── 事件: resize → 相机/渲染器自适应
└── 清理: cancelAnimationFrame + dispose + removeChild
```

**配色方案**：

| 颜色 | Hex | 占比 | 说明 |
|------|-----|------|------|
| 主色蓝 | `#4A90D9` | 35% | 小文品牌色 |
| 紫色 | `#7C5CFC` | 25% | 科技感 |
| 青绿 | `#00D4AA` | 20% | 点缀 |
| 白色星光 | `#FFFFFF` | 20% | 高亮粒子 |

**性能限制策略**：

```javascript
// 像素比限制：最高 2x，低端设备保持流畅
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

// 移动端降级（可选方案）
const isMobile = /Mobi|Android/i.test(navigator.userAgent);
const particleCount = isMobile ? 300 : 800;
```

**背景渐变叠加**（CSS 层，粒子层下面）：

```css
background: linear-gradient(
  135deg,
  #0F172A 0%,
  #1E293B 50%,
  #16213E 100%
);
```

### 4.3 验收标准

- [ ] 800 粒子在球形空间均匀分布
- [ ] 粒子自动缓慢旋转（每秒约 0.5 度）
- [ ] 鼠标移动时粒子团跟随偏移（lerp 平滑，无突兀跳变）
- [ ] 页面 resize 时场景自适应
- [ ] 组件卸载时正确清理（无内存泄漏）
- [ ] 控制台无 Three.js 警告/错误
- [ ] 首屏渲染负载 < 2s

---

## 五、阶段三：登录注册前端闭环（1 天）

### 5.1 任务清单

| 任务编号 | 任务 | 预估 | 产出物 |
|----------|------|------|--------|
| T3.1 | 创建 AuthContext | 2h | `src/contexts/AuthContext.jsx` |
| T3.2 | 改造 apiBase.js 自动注入 Token | 0.5h | `src/apiBase.js` |
| T3.3 | 创建 LoginPage 登录表单 UI | 2h | `src/components/LoginPage.jsx` |
| T3.4 | 整合 LoginParticleBg 到 LoginPage | 0.5h | 同上 |
| T3.5 | 创建 ProtectedRoute 路由守卫 | 1h | `src/components/ProtectedRoute.jsx` |
| T3.6 | 改造 main.jsx，包裹 AuthProvider | 1h | `src/main.jsx` |
| T3.7 | 实现退出登录功能 | 0.5h | AuthContext + Header 组件 |

### 5.2 T3.1：AuthContext 核心

```jsx
// src/contexts/AuthContext.jsx
import { createContext, useContext, useState, useEffect, useCallback } from 'react';
import { apiFetch } from '../apiBase';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const [token, setToken] = useState(() => localStorage.getItem('xiaowen-token'));
  const [loading, setLoading] = useState(true);

  // 初始化：验证已有 token
  useEffect(() => {
    if (!token) {
      setLoading(false);
      return;
    }
    apiFetch('/api/auth/me')
      .then(res => res.json())
      .then(data => {
        if (data.user) setUser(data.user);
        else logout();
      })
      .catch(() => logout())
      .finally(() => setLoading(false));
  }, [token]);

  const login = useCallback(async (username, password) => {
    const res = await apiFetch('/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password }),
    });
    const data = await res.json();
    if (data.token) {
      localStorage.setItem('xiaowen-token', data.token);
      setToken(data.token);
      setUser(data.user);
      return { success: true };
    }
    return { success: false, error: data.error };
  }, []);

  const register = useCallback(async (username, password, displayName) => {
    const res = await apiFetch('/api/auth/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password, displayName }),
    });
    const data = await res.json();
    if (data.token) {
      localStorage.setItem('xiaowen-token', data.token);
      setToken(data.token);
      setUser(data.user);
      return { success: true };
    }
    return { success: false, error: data.error };
  }, []);

  const logout = useCallback(() => {
    localStorage.removeItem('xiaowen-token');
    setToken(null);
    setUser(null);
  }, []);

  return (
    <AuthContext.Provider value={{ user, token, loading, login, register, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within AuthProvider');
  return ctx;
}
```

### 5.3 T3.2：改造 apiBase.js

```javascript
// src/apiBase.js
const API_BASE = import.meta.env.VITE_API_BASE || '/api';

export async function apiFetch(path, options = {}) {
  const token = localStorage.getItem('xiaowen-token');
  const headers = { ...options.headers };
  if (token) {
    headers['Authorization'] = `Bearer ${token}`;
  }
  return fetch(`${API_BASE}${path}`, { ...options, headers });
}
```

### 5.4 T3.3-3.4：LoginPage 组件

```jsx
// src/components/LoginPage.jsx
import { useState } from 'react';
import { useAuth } from '../contexts/AuthContext';
import LoginParticleBg from './LoginParticleBg';
import './LoginPage.css';

export default function LoginPage() {
  const { login, register } = useAuth();
  const [isRegister, setIsRegister] = useState(false);
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [displayName, setDisplayName] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

  const handleSubmit = async (e) => {
    e.preventDefault();
    setError('');
    setLoading(true);
    const fn = isRegister ? register : login;
    const args = isRegister
      ? [username, password, displayName]
      : [username, password];
    const result = await fn(...args);
    setLoading(false);
    if (!result.success) setError(result.error);
  };

  return (
    <div className="login-page">
      <LoginParticleBg />
      <div className="login-card">
        <div className="login-header">
          <div className="login-logo-icon">W</div>
          <h1>小文智能助手</h1>
          <p>你的桌面 AI 伙伴</p>
        </div>

        <form onSubmit={handleSubmit} className="login-form">
          {error && <div className="login-error">{error}</div>}

          <input
            type="text"
            placeholder="用户名"
            value={username}
            onChange={e => setUsername(e.target.value)}
            autoFocus
          />
          <input
            type="password"
            placeholder="密码"
            value={password}
            onChange={e => setPassword(e.target.value)}
          />

          {isRegister && (
            <input
              type="text"
              placeholder="显示名称（可选）"
              value={displayName}
              onChange={e => setDisplayName(e.target.value)}
            />
          )}

          <button type="submit" disabled={loading}>
            {loading ? '处理中...' : isRegister ? '注册' : '登录'}
          </button>
        </form>

        <div className="login-switch">
          {isRegister ? '已有账号？' : '没有账号？'}
          <button type="button" onClick={() => { setIsRegister(!isRegister); setError(''); }}>
            {isRegister ? '去登录' : '去注册'}
          </button>
        </div>
      </div>
    </div>
  );
}
```

### 5.5 T3.5：ProtectedRoute 路由守卫

```jsx
// src/components/ProtectedRoute.jsx
import { useAuth } from '../contexts/AuthContext';
import LoginPage from './LoginPage';

export default function ProtectedRoute({ children }) {
  const { user, loading } = useAuth();

  if (loading) {
    return (
      <div className="loading-screen">
        <div className="loading-spinner" />
        <p>加载中...</p>
      </div>
    );
  }

  if (!user) {
    return <LoginPage />;
  }

  return children;
}
```

### 5.6 T3.6：改造 main.jsx

```jsx
// src/main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { AuthProvider } from './contexts/AuthContext';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuthProvider>
      <App />
    </AuthProvider>
  </React.StrictMode>
);
```

在 `App.jsx` 中使用路由守卫：

```jsx
// src/App.jsx
import ProtectedRoute from './components/ProtectedRoute';
import MainLayout from './components/MainLayout';

export default function App() {
  return (
    <ProtectedRoute>
      <MainLayout />
    </ProtectedRoute>
  );
}
```

### 5.7 验收标准

- [ ] 无 token 时显示登录页
- [ ] 注册成功后自动登录并跳转主界面
- [ ] 登录成功后 token 持久化到 localStorage
- [ ] 刷新页面自动恢复登录态（`/api/auth/me` 验证）
- [ ] 登录/注册表单校验（空值、用户名重复、密码错误）
- [ ] 点击退出登录清除 token 并回到登录页
- [ ] 未认证时调用业务 API 返回 401

---

## 六、阶段四：个人偏好系统（1.5 天）

### 6.1 任务清单

| 任务编号 | 任务 | 预估 | 产出物 |
|----------|------|------|--------|
| T4.1 | 后端偏好 API（GET/PUT）| 2h | `backend/routes/preferences.py` |
| T4.2 | 偏好注入系统提示 | 1.5h | `backend/logic/task_parser.py` 改造 |
| T4.3 | 前端设置面板 UI | 3h | `src/components/SettingsPanel.jsx` |
| T4.4 | 偏好持久化同步到后端 | 1h | ThemeContext 联动改造 |
| T4.5 | 首次登录引导设置偏好 | 1h | 引导弹窗组件 |

### 6.2 偏好字段设计

| 字段 | 键名 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| 称呼 | `display_name` | text | `""` | 让小文如何称呼用户 |
| 职业/角色 | `role` | text | `""` | 如"前端工程师"、"学生" |
| 兴趣领域 | `interests` | text | `""` | 逗号分隔，如"编程,摄影" |
| 回复风格 | `language_style` | select | `casual` | casual / formal / concise |
| 回复长度 | `response_length` | select | `medium` | short / medium / long |
| TTS 音色 | `tts_voice` | select | `default` | 默认 / 温柔女声 / 沉稳男声 |

### 6.3 T4.1：后端偏好 API

```python
# backend/routes/preferences.py
from flask import Blueprint, request, jsonify
from flask_jwt_extended import jwt_required, get_jwt_identity
from models.user import get_db

pref_bp = Blueprint("preferences", __name__, url_prefix="/api/preferences")

DEFAULT_PREFERENCES = {
    "display_name": "",
    "role": "",
    "interests": "",
    "language_style": "casual",
    "response_length": "medium",
    "tts_voice": "default",
}

@pref_bp.route("", methods=["GET"])
@jwt_required()
def get_preferences():
    user_id = int(get_jwt_identity())
    conn = get_db()
    rows = conn.execute(
        "SELECT pref_key, pref_value FROM user_preferences WHERE user_id = ?",
        (user_id,)
    ).fetchall()
    conn.close()
    prefs = dict(DEFAULT_PREFERENCES)
    for row in rows:
        prefs[row["pref_key"]] = row["pref_value"]
    return jsonify({"preferences": prefs})

@pref_bp.route("", methods=["PUT"])
@jwt_required()
def update_preferences():
    user_id = int(get_jwt_identity())
    data = request.get_json()
    conn = get_db()
    for key, value in data.items():
        if key in DEFAULT_PREFERENCES:
            conn.execute(
                """INSERT INTO user_preferences (user_id, pref_key, pref_value)
                   VALUES (?, ?, ?)
                   ON CONFLICT(user_id, pref_key)
                   DO UPDATE SET pref_value = ?, updated_at = datetime('now','localtime')""",
                (user_id, key, str(value), str(value))
            )
    conn.commit()
    conn.close()
    return jsonify({"status": "ok"})
```

### 6.4 T4.2：偏好注入系统提示

改造 `backend/logic/task_parser.py` 中的 `handle_chat`：

```python
# backend/logic/task_parser.py 新增
def get_user_preferences(user_id: int) -> dict:
    conn = get_db()
    rows = conn.execute(
        "SELECT pref_key, pref_value FROM user_preferences WHERE user_id = ?",
        (user_id,)
    ).fetchall()
    conn.close()
    return {row["pref_key"]: row["pref_value"] for row in rows}

def build_personalized_system_prompt(user_id: int) -> str:
    """构建包含用户偏好的系统提示"""
    prefs = get_user_preferences(user_id)
    base = SYSTEM_PROMPT

    lines = []
    if prefs.get("display_name"):
        lines.append(f"- 用户称呼：{prefs['display_name']}")
    if prefs.get("role"):
        lines.append(f"- 用户职业/角色：{prefs['role']}，请据此调整回复的专业程度和术语")
    if prefs.get("interests"):
        lines.append(f"- 用户兴趣：{prefs['interests']}，可在合适时引用")
    if prefs.get("language_style"):
        style_map = {
            "casual": "- 请用轻松随意的语气回复",
            "formal": "- 请用正式专业的语气回复",
            "concise": "- 请用简洁精炼的语气回复，尽量短",
        }
        lines.append(style_map.get(prefs["language_style"], ""))

    if lines:
        base += "\n\n## 用户偏好\n" + "\n".join(lines)

    return base

# handle_chat 中调用
def handle_chat(task, user_id, session_id="default", location=None):
    personalized_prompt = build_personalized_system_prompt(user_id)
    messages = [{"role": "system", "content": personalized_prompt}]
    # ... 后续逻辑
```

### 6.5 T4.3：设置面板 UI

```jsx
// src/components/SettingsPanel.jsx
import { useState, useEffect } from 'react';
import { apiFetch } from '../apiBase';
import './SettingsPanel.css';

const FIELDS = [
  {
    key: 'display_name', label: '称呼',
    type: 'text', placeholder: '让小文怎么称呼你',
  },
  {
    key: 'role', label: '职业/角色',
    type: 'text', placeholder: '如：前端工程师、学生',
  },
  {
    key: 'interests', label: '兴趣领域',
    type: 'text', placeholder: '如：编程、摄影、音乐',
  },
  {
    key: 'language_style', label: '回复风格',
    type: 'select',
    options: [
      { value: 'casual', label: '轻松随意' },
      { value: 'formal', label: '正式专业' },
      { value: 'concise', label: '简洁精炼' },
    ],
  },
  {
    key: 'response_length', label: '回复长度',
    type: 'select',
    options: [
      { value: 'short', label: '简短' },
      { value: 'medium', label: '适中' },
      { value: 'long', label: '详细' },
    ],
  },
  {
    key: 'tts_voice', label: 'TTS 音色',
    type: 'select',
    options: [
      { value: 'default', label: '默认' },
      { value: 'gentle_female', label: '温柔女声' },
      { value: 'steady_male', label: '沉稳男声' },
    ],
  },
];

export default function SettingsPanel({ onClose }) {
  const [prefs, setPrefs] = useState({});
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState('');

  useEffect(() => {
    apiFetch('/api/preferences')
      .then(res => res.json())
      .then(data => setPrefs(data.preferences || {}));
  }, []);

  const handleChange = (key, value) => {
    setPrefs(prev => ({ ...prev, [key]: value }));
  };

  const handleSave = async () => {
    setSaving(true);
    setMessage('');
    try {
      await apiFetch('/api/preferences', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(prefs),
      });
      setMessage('保存成功');
      setTimeout(() => { onClose?.(); }, 800);
    } catch {
      setMessage('保存失败');
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="settings-overlay" onClick={onClose}>
      <div className="settings-panel" onClick={e => e.stopPropagation()}>
        <div className="settings-header">
          <h3>个人偏好设置</h3>
          <p>这些设置将影响小文的回复风格</p>
        </div>

        <div className="settings-body">
          {FIELDS.map(field => (
            <div className="settings-field" key={field.key}>
              <label htmlFor={`pref-${field.key}`}>{field.label}</label>
              {field.type === 'text' ? (
                <input
                  id={`pref-${field.key}`}
                  type="text"
                  value={prefs[field.key] || ''}
                  onChange={e => handleChange(field.key, e.target.value)}
                  placeholder={field.placeholder}
                />
              ) : (
                <select
                  id={`pref-${field.key}`}
                  value={prefs[field.key] || field.options[0].value}
                  onChange={e => handleChange(field.key, e.target.value)}
                >
                  {field.options.map(opt => (
                    <option key={opt.value} value={opt.value}>{opt.label}</option>
                  ))}
                </select>
              )}
            </div>
          ))}
        </div>

        {message && <div className="settings-message">{message}</div>}

        <div className="settings-footer">
          <button onClick={onClose} className="btn-secondary">取消</button>
          <button onClick={handleSave} disabled={saving} className="btn-primary">
            {saving ? '保存中...' : '保存'}
          </button>
        </div>
      </div>
    </div>
  );
}
```

### 6.6 验收标准

- [ ] 设置面板可打开/关闭（弹窗/抽屉模式）
- [ ] 6 项偏好均可编辑和保存
- [ ] 保存后偏好同步到后端，刷新不丢失
- [ ] 偏好保存后，下一次对话 system prompt 包含偏好信息
- [ ] 修改回复风格后小文回复语气确实变化
- [ ] 设置面板样式符合整体 UI 风格

---

## 七、阶段五：用户日志系统（1.5 天）

### 7.1 任务清单

| 任务编号 | 任务 | 预估 | 产出物 |
|----------|------|------|--------|
| T5.1 | 后端日志写入逻辑（对话自动记录） | 2h | 改造 `backend/routes/api.py` |
| T5.2 | 后端日志查询 API（分页、按会话） | 2h | `backend/routes/logs.py` |
| T5.3 | 前端日志面板改造（虚拟滚动 + 按 user 隔离） | 3h | `src/components/LogPanel.jsx` |
| T5.4 | 日志面板入口与交互（侧边栏/底部抽屉） | 1.5h | Layout 改造 |

### 7.2 T5.1：对话自动记录

在 `send_task` 中自动记录每一轮对话：

```python
@api_bp.route("/send-task", methods=["POST"])
@jwt_required()
def send_task():
    user_id = int(get_jwt_identity())
    data = request.get_json()
    task = data.get("task", "").strip()
    session_id = data.get("sessionId", "default")

    # 1. 记录用户输入
    save_log(user_id, session_id, "user", task, intent=None)

    # 2. 意图分类 & 处理
    intent_name, confidence = intent_classifier.classify(task)
    result = handler(task, session_id=session_id, location=location)

    # 3. 记录助手回复
    save_log(user_id, session_id, "assistant", result.get("msg", ""), intent=intent_name)

    return jsonify(result)


def save_log(user_id, session_id, role, content, intent=None):
    conn = get_db()
    conn.execute(
        """INSERT INTO user_logs (user_id, session_id, role, content, intent)
           VALUES (?, ?, ?, ?, ?)""",
        (user_id, session_id, role, content, intent or "")
    )
    conn.commit()
    conn.close()
```

### 7.3 T5.2：日志查询 API

```python
# backend/routes/logs.py
from flask import Blueprint, request, jsonify
from flask_jwt_extended import jwt_required, get_jwt_identity
from models.user import get_db

logs_bp = Blueprint("logs", __name__, url_prefix="/api/logs")

@logs_bp.route("", methods=["GET"])
@jwt_required()
def get_logs():
    """获取当前用户的日志，支持分页和按会话筛选"""
    user_id = int(get_jwt_identity())
    session_id = request.args.get("sessionId")
    page = int(request.args.get("page", 1))
    page_size = int(request.args.get("pageSize", 50))
    offset = (page - 1) * page_size

    conn = get_db()

    # 查询日志
    if session_id:
        rows = conn.execute(
            """SELECT * FROM user_logs
               WHERE user_id = ? AND session_id = ?
               ORDER BY created_at DESC
               LIMIT ? OFFSET ?""",
            (user_id, session_id, page_size, offset)
        ).fetchall()
        total = conn.execute(
            "SELECT COUNT(*) FROM user_logs WHERE user_id = ? AND session_id = ?",
            (user_id, session_id)
        ).fetchone()[0]
    else:
        rows = conn.execute(
            """SELECT * FROM user_logs
               WHERE user_id = ?
               ORDER BY created_at DESC
               LIMIT ? OFFSET ?""",
            (user_id, page_size, offset)
        ).fetchall()
        total = conn.execute(
            "SELECT COUNT(*) FROM user_logs WHERE user_id = ?",
            (user_id,)
        ).fetchone()[0]

    # 查询会话列表（按时间倒序）
    sessions = conn.execute(
        """SELECT DISTINCT session_id,
                  MIN(created_at) as first_at,
                  MAX(created_at) as last_at,
                  COUNT(*) as turn_count
           FROM user_logs
           WHERE user_id = ?
           GROUP BY session_id
           ORDER BY last_at DESC""",
        (user_id,)
    ).fetchall()

    conn.close()

    return jsonify({
        "logs": [dict(r) for r in rows],
        "sessions": [dict(s) for s in sessions],
        "total": total,
        "page": page,
        "pageSize": page_size,
    })
```

### 7.4 T5.3：日志面板虚拟滚动改造

```jsx
// src/components/LogPanel.jsx
import { useRef, useEffect, useState, useCallback } from 'react';
import { useVirtualizer } from '@tanstack/react-virtual';
import { apiFetch } from '../apiBase';
import './LogPanel.css';

export default function LogPanel() {
  const [logs, setLogs] = useState([]);
  const [page, setPage] = useState(1);
  const [hasMore, setHasMore] = useState(true);
  const parentRef = useRef(null);

  // 首次加载
  useEffect(() => {
    loadLogs(1);
  }, []);

  const loadLogs = async (pageNum) => {
    const res = await apiFetch(`/api/logs?page=${pageNum}&pageSize=50`);
    const data = await res.json();
    if (pageNum === 1) {
      setLogs(data.logs);
    } else {
      setLogs(prev => [...prev, ...data.logs]);
    }
    setHasMore(data.logs.length === 50);
    setPage(pageNum);
  };

  const loadMore = () => {
    if (hasMore) loadLogs(page + 1);
  };

  // 估算行高
  const estimateSize = useCallback((index) => {
    const log = logs[index];
    const len = log?.content?.length || 0;
    if (len > 200) return 100;
    if (len > 100) return 72;
    return 48;
  }, [logs]);

  const virtualizer = useVirtualizer({
    count: logs.length,
    getScrollElement: () => parentRef.current,
    estimateSize,
    overscan: 5,
  });

  return (
    <div ref={parentRef} className="log-panel" style={{ overflow: 'auto', height: '100%' }}>
      <div style={{ height: virtualizer.getTotalSize(), position: 'relative' }}>
        <div
          style={{
            position: 'absolute', top: 0, left: 0, width: '100%',
            transform: `translateY(${virtualizer.getVirtualItems()[0]?.start ?? 0}px)`,
          }}
        >
          {virtualizer.getVirtualItems().map(vItem => {
            const log = logs[vItem.index];
            return (
              <div
                key={log.id}
                ref={virtualizer.measureElement}
                data-index={vItem.index}
                className={`log-item log-${log.role}`}
              >
                <span className="log-time">
                  {new Date(log.created_at).toLocaleTimeString('zh-CN')}
                </span>
                <span className="log-role">{log.role === 'user' ? '你' : '小文'}</span>
                <span className="log-content">{log.content}</span>
                {log.intent && <span className="log-intent">{log.intent}</span>}
              </div>
            );
          })}
        </div>
      </div>

      {hasMore && (
        <button className="log-load-more" onClick={loadMore}>
          加载更多...
        </button>
      )}
    </div>
  );
}
```

### 7.5 验收标准

- [ ] 每轮对话自动写入 `user_logs` 表
- [ ] 按 user_id 严格隔离，每个用户只看到自己的日志
- [ ] 日志面板支持虚拟滚动，万条日志流畅滚动
- [ ] 支持分页加载（首次 50 条，滚动到底加载更多）
- [ ] 日志显示时间、角色、内容、意图标签
- [ ] 日志面板有入口按钮（Header 或侧边栏）
- [ ] 退出登录后日志面板不可见

---

## 八、阶段六：联调与集成测试（1 天）

### 8.1 任务清单

| 任务编号 | 任务 | 预估 |
|----------|------|------|
| T6.1 | 全流程走查：注册 → 登录 → 对话 → 偏好设置 → 日志查看 → 退出 | 1.5h |
| T6.2 | 多用户隔离测试：两个账号对话互不干扰 | 1h |
| T6.3 | 边界测试：Token 过期、网络断开、空值输入 | 1h |
| T6.4 | 样式联调：登录页→主界面过渡动画、深色主题适配 | 1.5h |
| T6.5 | 性能测试：日志万条滚动、3D 动画首屏加载 | 1h |
| T6.6 | Bug 修复与代码清理 | 1.5h |

### 8.2 全流程走查清单

```
[ ] 注册新用户 → 自动登录 → 进入主界面
[ ] 登录已有用户 → 恢复上次会话
[ ] 刷新页面 → 自动恢复登录态
[ ] 退出登录 → 回到登录页 → 无法访问 API
[ ] 登录页 3D 粒子动画正常（自转 + 鼠标视差）
[ ] 登录/注册表单校验：空值提示、重复用户名提示
[ ] 打开设置面板 → 修改称呼/职业/回复风格 → 保存
[ ] 发送对话 → 小文回复体现偏好（如称呼、风格）
[ ] 对话自动记录到日志
[ ] 打开日志面板 → 查看历史对话 → 滚动加载更多
[ ] 切换到另一个账号 → 看不到前一个账号的日志
[ ] 主题切换（深色/浅色）在登录页和主界面均正常
[ ] 页面 resize → 3D 动画自适应
[ ] 控制台无报错
```

---

## 九、总时间线与里程碑

```
Day 1          Day 2          Day 3          Day 4          Day 5
├──────────────┼──────────────┼──────────────┼──────────────┤
│  阶段一      │  阶段一      │  阶段四      │  阶段五      │
│  (后端认证)  │  (完成)      │  (偏好系统)  │  (日志系统)  │
│              │              │              │              │
│              │  阶段二      │  阶段四      │  阶段五      │
│              │  (3D 动画)   │  (完成)      │  (完成)      │
│              │              │              │              │
│              │  阶段三      │              │  阶段六      │
│              │  (登录闭环)  │              │  (联调测试)  │
└──────────────┴──────────────┴──────────────┴──────────────┘

里程碑 M1：Day 2 结束 → 后端认证可用 + 登录页 3D 动画可预览
里程碑 M2：Day 3 结束 → 用户可登录注册 + 3D 登录页完整
里程碑 M3：Day 4 结束 → 个人偏好 + 日志系统完成
里程碑 M4：Day 5 结束 → 全功能联调通过，可演示
```

### 每日任务分布

| 日期 | 上午（4h） | 下午（4h） |
|------|-----------|-----------|
| **Day 1** | T1.1-T1.3：SQLite 建表 + register/login API | T1.4-T1.7：me API + app.py 改造 + 保护 API |
| **Day 2** | T2.1-T2.3：Three.js 粒子场景 + 鼠标视差 | T3.1-T3.2：AuthContext + apiBase 改造 |
| **Day 3** | T3.3-T3.6：LoginPage 整合 + 路由守卫 | T3.7 + T2.4-T2.5：退出登录 + 动画优化 |
| **Day 4** | T4.1-T4.2：偏好 API + 系统提示注入 | T4.3-T4.5：设置面板 UI + 首次引导 |
| **Day 5** | T5.1-T5.3：日志写入 + 查询 API + 虚拟滚动 | T6.1-T6.6：联调测试 |

---

## 十、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| **Three.js 首屏加载慢** | 中 | 中 | CDN 异步加载 + 移动端粒子减半 + loading 占位 |
| **低端设备 3D 动画卡顿** | 中 | 低 | 自动检测 FPS，低于 30 时降级为纯 CSS 渐变背景 |
| **JWT 密钥泄露** | 低 | 高 | `.env` 独立管理，不提交 git；生产环境换强密钥 |
| **SQLite 并发写入** | 低 | 中 | PRAGMA WAL 模式 + 写操作加简短锁；后期升 PostgreSQL |
| **偏好注入导致 prompt 过长** | 低 | 中 | 偏好字段精简为一行文本，不为空才注入 |
| **日志表膨胀** | 中 | 低 | 按时间分区索引 + 后续可加自动清理策略（如保留 90 天） |
| **登录页与主界面过渡生硬** | 低 | 低 | 加 fadeOut/in 过渡动画（CSS transition 0.3s） |

---

> **下一步**：确认本计划后，按 Day 1 的任务开始执行。建议每完成一个阶段做一次 `git commit`，方便回滚。
