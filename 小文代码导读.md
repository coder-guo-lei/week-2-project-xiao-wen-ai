# 小文项目代码导读（前后端）

> **说明**：仓库里 `backend/logic/task_parser.py` 等文件数千行，若对「每一行」都写注释会难以阅读与合并代码。做法是：**入口与小文件逐行/逐块注释** + **本导读按文件与数据流说明** + **`task_parser.py` 内章节索引**。

---

## 一、从用户操作到后端再回到前端

1. 浏览器 `App.jsx` 里用户输入或语音转文字 → `fetch('http://127.0.0.1:5001/api/send-task', { body: { task, history, location } })`。
2. Flask `routes/api.py` 的 `send_task()` 取出 `task`，调用 `logic.task_parser.parse_command(...)`。
3. `parse_command` 按**固定顺序**匹配意图（告别、模拟世界、应用列表、知识库、天气、音乐……），命中则返回 `{ type, msg, ...可选字段 }`。
4. `send_task` 把 `reply/type` 以及 `previewUrl`、`chartData`、`workflow` 等**有则拷贝**进 JSON 响应。
5. 前端根据 `type` 切换 `contentType`（`default` / `chat` / `weather` / `music` / `image` / `chart`），并更新 `ModeBar`、`WorkflowPanel` 等。

朗读：`ChatPanel` → `POST /api/tts` → `routes/api.py` 的 `tts_xfyun` → `services/xfyun_tts.py`。

---

## 二、后端文件（建议阅读顺序）

| 文件 | 作用 |
|------|------|
| `backend/app.py` | 创建 Flask、注册 CORS、挂载 `api_bp`、启动端口 5001。**已加逐行级中文注释。** |
| `backend/config.py` | `load_dotenv`、API Key、模型名、`APP_LAUNCHERS`、微信/QQ 路径解析函数。常量名即文档。 |
| `backend/routes/api.py` | 全部 `/api/*`。**已为模块与各路由补充说明注释。** |
| `backend/logic/task_parser.py` | 核心业务与 `parse_command`。**文件顶部有章节索引注释**；各节以 `# ---------- N.` 分隔。 |
| `backend/services/xfyun_tts.py` | 讯飞 WebSocket 合成。 |
| `backend/services/xfyun_iat.py` | 讯飞听写（WAV → 文本）。 |
| `backend/session.py` | 进程内会话状态（如模拟世界）。 |

---

## 三、`parse_command` 之前应了解的事

- **顺序敏感**：例如先判断「再见」再判断普通聊天，避免误匹配。
- **返回字典**：至少含 `msg`（展示文案）、`type`（前端路由类型）；`with_workflow(...)` 会附加 `workflow` 步骤给 UI。

具体分支请直接在 `task_parser.py` 内搜索 `def parse_command` 并向下阅读 `if` 链。

---

## 四、前端文件（建议阅读顺序）

| 文件 | 作用 |
|------|------|
| `xiao-wen-ai/src/main.jsx` | ReactDOM 挂载入口。**已加注释。** |
| `xiao-wen-ai/src/App.jsx` | 全局状态、发送指令、轮询文生图、音乐 Hook、语音 Hook。**文件头已有模块说明；下方按「状态 / 回调 / JSX」三块阅读。** |
| `xiao-wen-ai/src/index.css` | 全局设计变量（主题色、深浅色、工具卡变量）。 |
| `xiao-wen-ai/src/hooks/useVoiceRecognition.js` | 唤醒词 + 单次指令识别（Web Speech / 麦克风策略）。 |
| `xiao-wen-ai/src/hooks/useMusicPlayer.js` | 音乐播放状态。 |
| `xiao-wen-ai/src/components/CommandInput.jsx` | 输入框与语音/唤醒/发送按钮。 |
| `xiao-wen-ai/src/components/ChatPanel.jsx` | 展示 `chatReply`、朗读、划句。 |
| `xiao-wen-ai/src/utils/xfyunTts.js` | 请求 `/api/tts` 并播放返回音频。 |

### `App.jsx` 状态一览（便于对照源码）

| state | 含义 |
|--------|------|
| `task` | 当前输入框文本 |
| `logList` | 右侧运行日志字符串列表 |
| `contentType` | 左栏主区域：`default` / `chat` / `weather` / `music` / `image` / `chart` |
| `chatReply` | `ChatPanel` 展示的正文 |
| `chatHistory` | 发给后端的 `history`（localStorage 持久化） |
| `weatherData` / `imageUrl` / `chartData` | 各模式专用数据 |
| `mode` / `modeLabel` | 顶栏模式条 |
| `workflowSteps` | 工作流步骤条 |
| `isSending` / `isAnalyzingImage` / `isGeneratingChart` | 防重复提交 |

---

## 五、若你仍希望「某一段源码」逐行讲解

请指定**文件路径 + 起止行号或函数名**（例如「`parse_command` 里音乐分支」），可以在不污染整文件的前提下，为该段补充注释或单独写一小节说明。
