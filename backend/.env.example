# 复制本文件为 .env 后填写（不要把真实密钥提交到 Git）

# 抖音 PC 客户端路径（可选）。自动扫描找不到 Douyin.exe 时再填，例如：
# DOUYIN_EXE_PATH=D:\1\douyin\douyin.exe

# 对话里「今天」「现在」用的本地时区（默认 Asia/Shanghai）
# LOCAL_TIMEZONE=Asia/Shanghai

# 意图分类：LLM + 规则兜底 + 缓存（默认开启 LLM；无 Key 时自动纯规则）
# INTENT_LLM_ENABLED=1
# INTENT_CACHE_TTL=3600
# INTENT_CACHE_MAX=512
# INTENT_RULE_CONFIDENCE=0.85

# 对话管理：滑动窗口 + 摘要压缩 + 状态机
# DIALOGUE_WINDOW_MESSAGES=8
# DIALOGUE_STORE_MESSAGES=24
# DIALOGUE_COMPRESS_MIN_MESSAGES=14
# DIALOGUE_COMPRESS_ENABLED=1
# DIALOGUE_STATE_HINTS=1

# 高德地图 Web 服务（天气、地理编码等）https://console.amap.com/
AMAP_KEY=

# 阿里云 DashScope / 百炼 — 对话、文生图、图片理解、划词翻译（全套能力）
DASHSCOPE_API_KEY=
# DASHSCOPE_CHAT_MODEL=qwen-turbo
# DASHSCOPE_VL_MODEL=qwen-vl-plus

# DeepSeek — OpenAI 兼容接口，可用于对话、翻译、知识库问答（不能替代百炼的生图/看图）
DEEPSEEK_API_KEY=
# DEEPSEEK_CHAT_URL=https://api.deepseek.com/v1/chat/completions
# DEEPSEEK_CHAT_MODEL=deepseek-chat

# 讯飞开放平台 — 在线语音合成 v2（应用 xiaowenai）https://console.xfyun.cn/
XFYUN_APP_ID=
XFYUN_API_KEY=
XFYUN_API_SECRET=
# 默认女声 vcn（控制台「基础发音人」→ 参数列），例如 x4_xiaoyan
# XFYUN_DEFAULT_VCN_FEMALE=x4_xiaoyan
# 基础发音人请用 classic；填 super 且未配超拟人 URL 时仍走在线 v2
# XFYUN_TTS_ENGINE=classic
# 已开通示例：x4_xiaoyan 小燕 | x4_yezi 小露 | aisjiuxu 许久 | aisjinger 小婧 | aisbabyxu 许小宝
# 语音听写（流式版）与 TTS 共用上述密钥；需在控制台开通「语音听写（流式版）」服务，否则返回业务错误码。

# —— 超拟人语音合成（可选，与在线合成 v2 二选一）：填写后 /api/tts 走超拟人协议
# 仅使用「在线语音合成」时不要填写 XFYUN_SUPER_TTS_WS_URL
# XFYUN_SUPER_TTS_WS_URL=wss://cbm01.cn-huabei-1.xf-yun.com/v1/private/你的实例路径
# XFYUN_SUPER_TTS_SAMPLE_RATE=24000
# XFYUN_SUPER_TEXT_UTF8_MAX=62000
# 口语化：high / mid / low；若报错可试 XFYUN_SUPER_ORAL_DISABLE=1 去掉 oral 块
# XFYUN_SUPER_ORAL_LEVEL=mid
# 填写超拟人 URL 后，默认女声为文档「默认免费发音人」聆小璇，可用下面覆盖：
# XFYUN_DEFAULT_VCN_FEMALE=x5_lingxiaoxuan_flow
# 若控制台未给该 vcn「超拟人」授权（11200），后端会自动降级为在线合成 v2。
# 未配置超拟人 URL 时，默认女声见 XFYUN_DEFAULT_VCN_FEMALE（如 x4_xiaoyan）
# XFYUN_TTS_SPEED=50
# XFYUN_TTS_VOLUME=50
# XFYUN_TTS_DISK_CACHE=1
# 默认 wav；多段拼接时不建议 mp3： XFYUN_TTS_AUDIO_FORMAT=mp3
# 仅在线合成 v2：采样率钳 8k/16k — XFYUN_TTS_SAMPLE_RATE=16000
